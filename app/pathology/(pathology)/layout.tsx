import React from "react";

const pathologyLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <html>
      <body>
        pathologyLayout
        {children}
      </body>
    </html>
  );
};

export default pathologyLayout;
